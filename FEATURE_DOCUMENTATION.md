# Feature documentation

Generated by `src/day05_review.py`. Regenerate with `python run_all.py --day 5`.

## 1. Purpose and scope
Features for predicting **`churn_30d`** (customer churns within 30 days after the snapshot date
2024-06-30) for a subscription service. Positive class is rare (~10.5%), so **PR-AUC** is the
headline metric; ROC-AUC and threshold metrics are secondary.

Final configuration: model = `logreg`, imbalance strategy = `none`,
feature set = `only_useful` (15 engineered features kept, 31 dropped),
operating threshold = 0.24 (chosen on out-of-fold train predictions).

## 2. Pipeline (all learning happens on training data only)
```
raw model frame (customers + event aggregates)
   -> FeatureEngineer            learns: rare-country list, promo-code frequencies, age quintile edges,
                                         price-pressure threshold          (unsupervised, never sees y)
   -> ColumnTransformer          numeric: median impute -> StandardScaler
                                 categorical: 'missing' impute -> OneHotEncoder(handle_unknown, min_frequency=1%)
   -> CorrelationFilter          drops constant columns and one of each pair with |r| > 0.95
   -> [resampler]                only if the chosen strategy needs one (fit-time only)
   -> model
```
* Everything above is one `imblearn.pipeline.Pipeline`; cross-validation clones it for every fold, so no
  statistic is ever shared between training and validation data.
* Event aggregates (`build_aggregate_features`) are per-customer, label-free and only use events dated on
  or before the snapshot; they are computed before the split because they cannot leak information across rows.
* No target encoding is used anywhere (categorical signal is captured by one-hot, frequency encoding and binning).
* Automated checks (10/10 passing): `fit_learns_only_from_training_data`, `sensitivity_check_leaky_fit_differs`, `features_do_not_use_target`, `scaler_fitted_on_training_data`, `handles_unseen_categories_and_missing_values`, `aggregates_ignore_events_after_snapshot`, `cv_uses_fresh_clone_per_fold`, `deterministic_refit`, `persistence_roundtrip`, `feature_catalog_complete`.

### Values learned from the training split (final pipeline)
* countries kept (others -> `other`): AE, CA, DE, IN, PK, SE, UK, US
* age quintile edges: 28, 35, 40, 47
* price-pressure threshold (75th percentile of `charge_per_event`): 0.967
* promo codes with a learned frequency: 126
* columns removed by the correlation filter: none

## 3. Review outcome
Permutation importance (drop in PR-AUC when a feature is shuffled) on 10 validation folds; each fold fits its own
feature engineer and model. Mean pairwise Spearman correlation of the importance rankings between folds: **0.52**.

* **Useful (15):** `recency_days`, `charge_x_recency`, `usage_state`, `log_charge_per_event`, `events_90d`, `active_days_30d`, `acquisition_channel`, `plan`, `usage_trend_ratio`, `premium_high_tickets`, `tickets_x_recency`, `purchase_amount_mean`, `country`, `monthly_charge`, `n_support_tickets`
* **Unstable / weak (11):** `purchase_amount_sum`, `charge_per_event`, `purchase_amount_max`, `basic_new_customer`, `purchase_count_90d`, `tenure_days`, `age_missing`, `tickets_per_month`, `signup_is_weekend`, `events_30d`, `login_share`
* **No signal - candidates to drop (20):** `is_new_customer`, `signup_month_sin`, `events_7d`, `weekend_share`, `events_per_tenure_day`, `log_monthly_charge`, `events_prior_60d`, `legacy_score`, `price_pressure`, `age`, `longest_gap_days`, `promo_freq`, `signup_month_cos`, `night_share`, `gender`, `event_type_diversity`, `paid_search_low_usage`, `age_band`, `device`, `tenure_log`

Rules (fixed before looking): useful = mean >= 0.002 and positive in >= 80% of folds;
drop = mean < 0.0005 or positive in < 50% of folds; otherwise unstable.

Candidate feature sets compared with 5-fold x 3-repeat CV (smallest set within 0.003 PR-AUC of the best is chosen):

| variant | n_engineered_features | pr_auc_mean | pr_auc_std | roc_auc_mean | f1_mean | chosen |
|---|---|---|---|---|---|---|
| full | 46 | 0.435 | 0.042 | 0.796 | 0.326 | False |
| prune_drop | 26 | 0.449 | 0.043 | 0.802 | 0.336 | False |
| only_useful | 15 | 0.456 | 0.043 | 0.804 | 0.343 | True |

> Caveat: permutation importance under-rates features that have correlated twins (for example `recency_days`,
> `longest_gap_days`, `active_days_30d`, `events_30d` all describe recent activity), which is why the pruned
> sets are validated by re-running cross-validation instead of trusting the verdict alone.

## 4. Feature dictionary

### base

| feature | type | definition | verdict | perm. importance | folds > 0 | mutual info |
|---|---|---|---|---|---|---|
| acquisition_channel | categorical | Marketing channel through which the customer signed up. | useful | 0.0194 | 0.9000 | 0.0024 |
| plan | categorical | Subscription plan: basic / standard / premium. | useful | 0.0177 | 1.0000 | 0.0034 |
| country | categorical | Country; countries below 2% of the training rows are grouped into 'other' (learned in fit). | useful | 0.0052 | 0.9000 | 0.0016 |
| monthly_charge | numeric | Monthly subscription price. | useful | 0.0045 | 0.8000 | 0.0054 |
| n_support_tickets | numeric | Number of support tickets ever opened. | useful | 0.0042 | 0.8000 | 0.0062 |
| legacy_score | numeric | Score from a legacy system (kept in the baseline on purpose; expected to be noise). | drop | -0.0001 | 0.7000 | 0.0047 |
| age | numeric | Customer age in years (missing values median-imputed inside the pipeline). | drop | -0.0003 | 0.3000 | 0.0000 |
| gender | categorical | Customer gender (one-hot encoded). | drop | -0.0007 | 0.4000 | 0.0001 |
| device | categorical | Main device; missing values become their own 'missing' level. | drop | -0.0015 | 0.3000 | 0.0003 |


### datetime

| feature | type | definition | verdict | perm. importance | folds > 0 | mutual info |
|---|---|---|---|---|---|---|
| tenure_days | numeric | Days between signup_date and the snapshot date. | unstable | 0.0012 | 0.6000 | 0.0030 |
| signup_is_weekend | numeric | 1 if the customer signed up on a Saturday or Sunday. | unstable | 0.0006 | 0.7000 | 0.0031 |
| is_new_customer | numeric | 1 if tenure_days < 90 else 0 (onboarding-period churn is different). | drop | 0.0001 | 0.6000 | 0.0000 |
| signup_month_sin | numeric | sin(2*pi*signup_month/12) - cyclical encoding of signup month. | drop | 0.0001 | 0.5000 | 0.0000 |
| signup_month_cos | numeric | cos(2*pi*signup_month/12) - cyclical encoding of signup month. | drop | -0.0004 | 0.6000 | 0.0000 |
| tenure_log | numeric | log(1 + tenure_days) - tames the right skew of tenure. | drop | -0.0016 | 0.4000 | 0.0035 |


### aggregate

| feature | type | definition | verdict | perm. importance | folds > 0 | mutual info |
|---|---|---|---|---|---|---|
| recency_days | numeric | Days since the most recent event (90 if no event in the window). | useful | 0.0825 | 1.0000 | 0.0538 |
| events_90d | numeric | Number of events (login/stream/purchase) in the 90 days before the snapshot. | useful | 0.0263 | 1.0000 | 0.0154 |
| active_days_30d | numeric | Distinct calendar days with at least one event in the last 30 days. | useful | 0.0195 | 0.9000 | 0.0519 |
| usage_trend_ratio | numeric | (events_30d/30) / (events_prior_60d/60 + 0.05): <1 means usage is declining. | useful | 0.0174 | 1.0000 | 0.0493 |
| purchase_amount_mean | numeric | Mean purchase amount in 90 days (0 if none). | useful | 0.0066 | 0.8000 | 0.0085 |
| purchase_amount_sum | numeric | Total purchase amount in 90 days. | unstable | 0.0048 | 0.6000 | 0.0072 |
| purchase_amount_max | numeric | Largest single purchase in 90 days (0 if none). | unstable | 0.0022 | 0.6000 | 0.0103 |
| purchase_count_90d | numeric | Number of purchase events in 90 days. | unstable | 0.0012 | 0.6000 | 0.0103 |
| events_30d | numeric | Events in the last 30 days. | unstable | 0.0006 | 0.6000 | 0.0534 |
| login_share | numeric | Share of events that are logins (missing if no events). | unstable | 0.0005 | 0.7000 | 0.0029 |
| events_7d | numeric | Events in the last 7 days. | drop | 0.0000 | 0.4000 | 0.0378 |
| weekend_share | numeric | Share of events on Saturday/Sunday (missing if no events). | drop | 0.0000 | 0.6000 | 0.0092 |
| events_prior_60d | numeric | Events between 90 and 30 days before the snapshot. | drop | 0.0000 | 0.0000 | 0.0149 |
| longest_gap_days | numeric | Longest inactivity gap (days) inside the 90-day window, including the gap up to the snapshot. | drop | -0.0003 | 0.5000 | 0.0278 |
| night_share | numeric | Share of events between 00:00 and 05:59 (missing if no events). | drop | -0.0006 | 0.3000 | 0.0107 |
| event_type_diversity | numeric | Number of distinct event types used in 90 days. | drop | -0.0009 | 0.6000 | 0.0038 |


### ratio

| feature | type | definition | verdict | perm. importance | folds > 0 | mutual info |
|---|---|---|---|---|---|---|
| log_charge_per_event | numeric | log(charge_per_event + 0.01). | useful | 0.0263 | 1.0000 | 0.0039 |
| charge_per_event | numeric | monthly_charge / (events_90d + 1): price paid per unit of usage. | unstable | 0.0044 | 0.6000 | 0.0036 |
| age_missing | numeric | 1 if age was missing (missingness indicator). | unstable | 0.0008 | 0.6000 | 0.0000 |
| tickets_per_month | numeric | n_support_tickets / (tenure_days/30 + 1): ticket rate normalised by lifetime. | unstable | 0.0007 | 0.6000 | 0.0052 |
| events_per_tenure_day | numeric | events_90d / min(tenure_days, 90): usage intensity, fair to new customers. | drop | 0.0000 | 0.0000 | 0.0138 |
| log_monthly_charge | numeric | log(monthly_charge). | drop | 0.0000 | 0.0000 | 0.0066 |


### interaction

| feature | type | definition | verdict | perm. importance | folds > 0 | mutual info |
|---|---|---|---|---|---|---|
| charge_x_recency | numeric | monthly_charge * recency_days. | useful | 0.0398 | 1.0000 | 0.0403 |
| premium_high_tickets | numeric | 1 if plan == premium AND n_support_tickets >= 3 (unhappy high-value customers). | useful | 0.0091 | 0.9000 | 0.0000 |
| tickets_x_recency | numeric | n_support_tickets * recency_days. | useful | 0.0079 | 1.0000 | 0.0388 |
| basic_new_customer | numeric | 1 if plan == basic AND tenure_days < 90. | unstable | 0.0012 | 0.9000 | 0.0000 |
| paid_search_low_usage | numeric | 1 if channel == paid_search AND usage_trend_ratio < 0.6. | drop | -0.0010 | 0.4000 | 0.0078 |


### categorical

| feature | type | definition | verdict | perm. importance | folds > 0 | mutual info |
|---|---|---|---|---|---|---|
| usage_state | categorical | Rule-based: 'declining' if usage_trend_ratio < 0.6, 'growing' if > 1.4, else 'stable'. | useful | 0.0313 | 1.0000 | 0.0346 |
| price_pressure | categorical | 'high' if charge_per_event is above the training 75th percentile, else 'normal'. | drop | -0.0002 | 0.5000 | 0.0029 |
| promo_freq | numeric | Frequency encoding of promo_code: share of training rows with that code (0 for unseen codes). No target encoding -> no leakage. | drop | -0.0004 | 0.6000 | 0.0034 |
| age_band | categorical | Age quintile band (q1..q5, 'unknown' if missing); quintile edges learned on training data. | drop | -0.0011 | 0.4000 | 0.0005 |


## 5. Reproducing
```bash
pip install -r requirements.txt
python run_all.py            # data -> Day 1..5 -> docs + final pipeline
pytest -q                    # leakage / reproducibility tests
```
Using the saved pipeline:
```python
import joblib, pandas as pd
from src.data import load_model_frame
pipe = joblib.load("outputs/final_pipeline.joblib")
X, y = load_model_frame()
churn_probability = pipe.predict_proba(X)[:, 1]
```
Versions, seeds and learned parameters are stored in `outputs/final_pipeline_config.json`.

## 6. Known limitations
* Data are synthetic: numbers show the *method*, not real-world performance. Replace `src/data.py` to use real data.
* Feature groups overlap in information (ratios, interactions and encodings are built from the same raw signals), so
  single-group ablations understate value; see the add-on-aggregate experiment in RESULTS_SUMMARY.md.
* Importance-based pruning used the same training folds as model selection, so the CV gain from pruning is optimistic.
  On the untouched hold-out set (threshold 0.5) PR-AUC is 0.374 for all features vs 0.368 for the final set:
  the pruned pipeline is smaller and simpler, with equal accuracy within noise (the hold-out has only about 130 positives).
